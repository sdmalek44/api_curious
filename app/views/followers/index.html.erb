<h2><%= current_user.name %>'s Followers</h2>
<% @presenter.followers.each do |follower| %>
<ul class="follower">
  <img src="<%= follower.image %>" alt="" class="follower-image">
  <li class="follower-login"><%= follower.login %></li>
</ul>
<% end %>
